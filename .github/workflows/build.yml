name: Build MT5 Agent EXE

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"
    
    - name: Install dependencies
      run: pip install pyinstaller websockets
    
    - name: Build EXE
      run: pyinstaller --onefile --noconsole agent.py
    
    - name: Upload EXE
      uses: actions/upload-artifact@v4
      with:
        name: MT5-Agent
        path: dist/agent.exe
